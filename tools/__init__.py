"""Standalone-CLI-Tools für den Scrappercontainer.

Aufruf per ``python -m tools.<name>`` aus dem Repo-Root (oder
/opt/scrapper im Container). Tools sind absichtlich NICHT Teil der
Web-App — sie sollen sich ad-hoc per SSH ausführen lassen.
"""
